#ifndef DISTANCE_UTIL_H
#define DISTANCE_UTIL_H

#include <string>
#include <vector>

std::string random_str(size_t n);
std::vector<std::string> random_strs(size_t N, size_t n);

#endif //DISTANCE_UTIL_H
